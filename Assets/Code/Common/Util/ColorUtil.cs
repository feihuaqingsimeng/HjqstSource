using UnityEngine;
namespace Common.Util
{
    public class ColorUtil
    {
        public static Color GetGrayColor(Color color)
        {
            //Color c = (color.r * 299 + color.g * 587 + color.b * 114 + 500) / 1000;
            return color;
        }
    }
}